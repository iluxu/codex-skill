{
  "name": "codex-skill",
  "version": "0.1.2",
  "description": "CLI for installing and managing Codex skills from a registry.",
  "type": "module",
  "bin": {
    "codex-skill": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist",
    "prepare": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.3.3"
  },
  "files": [
    "dist"
  ]
}
